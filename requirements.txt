# Heritage Ring — dipendenze Fase 1 (solo Shopify)
python-dotenv==1.0.1
requests==2.32.3
PyYAML==6.0.2
# supabase>=2.15 supporta le nuove API key (sb_secret_/sb_publishable_);
# le versioni <2.15 rifiutano la chiave perché pretendono il vecchio formato JWT.
supabase==2.30.1
python-telegram-bot==21.6
pytz==2024.2
# Anthropic: risposte libere in linguaggio naturale (MAI per i numeri)
anthropic==0.105.2
# Dashboard web (terzo servizio Railway, SOLA LETTURA). pandas arriva con streamlit.
streamlit==1.40.2
pandas==2.2.3
# Stripe (SOLA LETTURA): riconciliazione, fee reali, payout, dispute.
stripe==11.4.0
